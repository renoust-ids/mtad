{
  "name": "mtad",
  "version": "0.0.10",
  "description": "MTad - Tabular data viewer monorepo",
  "overrides": {
    "ws": "8.18.1"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "jest": "^29.7.0",
    "lerna": "^4.0.0",
    "rimraf": "^5.0.5",
    "run-script-os": "^1.1.6",
    "ts-jest": "^29.2.6",
    "typescript": "^4.8.3"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap --ci --hoist --force-local --ignore-prepublish",
    "bootstrap-no-drivers-no-app": "lerna bootstrap --ignore reltab-* --ignore tad --ci --hoist --force-local --ignore-prepublish"
  }
}
